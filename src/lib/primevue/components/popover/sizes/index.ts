const sizes = {
  padding: '{overlay.popover.padding}',
  gutter: '0.5rem',
  arrowOffset: '1.25rem',
  borderRadius: '{overlay.popover.borderRadius}',
  shadow: '{shadow.xs}',
};

export default sizes
